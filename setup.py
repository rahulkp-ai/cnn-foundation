"""
setup.py — present alongside pyproject.toml for compatibility with tooling
that still expects a setup.py entry point (mirrors ann-foundation's setup).
The actual build configuration lives in pyproject.toml.
"""

from setuptools import setup

setup()
